学习的智能体 和 与环境交互的智能体 是相同的：同策略
不同的：异策略

对于奖励梯度
![[Pasted image 20260406202143.png]]
一旦更新参数 $\theta$ 就必须重新采样，会花费很长的时间
希望变成异策略，用另一个策略 $\pi_{\theta'}$  采样得到的数据去训练 $\theta$ 
这样可以使用同一批数据，多次执行梯度上升

重要性采样 importance sampling
